# WeTrail frontend

This project is a Vue3 Nest JS frontend


## Installation

- Copy `.env.example` to `.env`
- Run `npm install`
-


## Run the application

- Start the frontend server: `npm run dev`
- You can consume your application by navigating to http://localhost:3000
